import React from "react";

// TODO: finish adding header to app for controlling language and other possible functions
const Header = () => {
  return <div className="app-header">HEADER</div>;
};

export default Header;
